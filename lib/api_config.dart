/// Centralized API configuration for the plane_borrow app.
/// Change [baseUrl] to match the IP/hostname where your Node.js server runs.
class ApiConfig {
  // Use '10.0.2.2' for Android emulator → localhost,
  // 'localhost' for iOS simulator / web,
  // or your machine's LAN IP for a physical device.
  static const String baseUrl = 'http://10.0.2.2:3000';
}
