# Copy to .env and fill in. .env is gitignored and must never be committed.
# Generate a strong secret, e.g.:  node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=plane_borrow
